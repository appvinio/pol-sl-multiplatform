import 'package:flutter/material.dart';
import 'package:flutter/widgets.dart';

class Shadows {
  static const textShadow = const BoxShadow(color: Colors.black, offset: Offset(1, 1), blurRadius: 2);
  static const degreeShadow = const BoxShadow(color: Colors.black, offset: Offset(1, 1), blurRadius: 2);
}
